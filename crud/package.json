{
  "name": "crud",
  "version": "1.0.0",
  "description": "A simple CAP project.",
  "repository": "<Add your repository here>",
  "license": "UNLICENSED",
  "private": true,
  "dependencies": {
    "@sap/cds": "^7",
    "express": "^4"
  },
  "devDependencies": {
    "@cap-js/sqlite": "^1"
  },
  
  "scripts": {
    "dev:back": "cds watch --port 4004",
    "dev:front": "cd app/crud && npm run start -- --proxy-config proxy.conf.json"
  },
  "cds": {
    "requires": {
      "db": {
        "kind": "sqlite",
        "credentials": {
          "url": "db/my-person.sqlite"
        }
      }
    },
    "folderes": {
      "db": "db/my-person.sqlite",
      "srv": "personRegister/",
      "app": "crud/"
    }
  }
}